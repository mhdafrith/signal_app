# ── Core ──────────────────────────────────────────────────────────────────────
fastapi>=0.95.0
uvicorn>=0.22.0
jinja2>=3.1.2
python-multipart>=0.0.6
pandas>=2.0.0
numpy>=1.24.0

# ── Optional: MDF/MF4 automotive format support ───────────────────────────────
# Uncomment if you need to open binary MDF files from CANape / INCA / MDA etc.
# asammdf>=7.3.0